{"findings":[{"id":"f-001","type":"ux_friction","severity":"medium","evidence":{"excerpt":"0 mutations — the shortcut might not have fired. Let me check focus and try clicking the canvas directly first:\n\nA textbox has focus — that's why `R` didn't trigger the shortcut, it typed into the textbox instead. I need to blur first, then press R."},"description":"Keyboard shortcuts can fail silently when an editable element still has focus. The agent had to infer the failure from zero mutations, inspect focus, and manually recover with blur/escape before retrying.","suggested_fix":"Add a preflight/warning path for `slop keys` in the extension: if the active element is editable and the requested keys look like an app shortcut, return focused-element info plus a clear hint to use `slop blur` or `slop keys --os` instead of reporting a generic success/no-op.","target":"extension","goal_alignment":"Serves Resilient and Fast by turning silent shortcut failures into actionable feedback and cutting retry loops."},{"id":"f-002","type":"workaround","severity":"medium","evidence":{"excerpt":"The `R` shortcut works with trusted events from MCP."},"description":"The agent discovered through trial and fallback that Canva accepted trusted key events, but not the path it was using first. This pushed the workflow out of slop and into MCP during a live task.","suggested_fix":"Update the slop skill/docs and CLI help to explicitly recommend `slop keys --os`, `slop click --os`, and `slop type --os` for apps like Canva/Figma/Docs that often ignore synthetic events. Include a short decision rule and one Canva example so agents use the existing trusted-input path first.","target":"docs","goal_alignment":"Serves Undetectable and Fast by keeping agents on slop-browser’s native trusted-input path and reducing fallback to alternate tooling."}]}